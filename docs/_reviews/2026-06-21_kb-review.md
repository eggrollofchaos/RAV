---
title: KB/Doc Review — RAV (knowledge-base) — 2026-06-21
slug: kb-review
type: report
status: live
created: 2026-06-21
updated: 2026-06-22
owner: Wei Alexander Xin
scope: project
project: RAV
---

# KB/Doc Review — RAV (knowledge-base) — 2026-06-21

> Review artifact / draft — NOT a KB entry. Recommendations only; nothing has been merged or deleted.

Scope reviewed: `docs/knowledge-base/` (recursive, markdown only). Three files across two layers (`decisions/`, `learnings/`). No `reference/` or `qa/` layer exists yet.

## Inventory

| Path | Purpose | Layer |
|------|---------|-------|
| `docs/knowledge-base/decisions/data-management.md` | Two ADRs: DEC-DAT-001 (chest-first CheXpert route for MVP) and DEC-DAT-002 (Kaggle pneumonia = POC, CheXpert-small = local primary). | decisions |
| `docs/knowledge-base/learnings/2026-05-29_rav-chexpert-ops.md` | CheXpert ops patterns: threshold-before-backbone, small-split smoke tests, GCP spot/disk/RUN_ID resume, local GPU lane, dataset-variant sizing, freshness gates. | learnings |
| `docs/knowledge-base/learnings/2026-06-04_rav-mvp-app-ops.md` | MVP app ops: interpreter-qualified Streamlit, missing-artifact-as-state, metrics provenance, LLM grounding, POC-vs-CheXpert claim separation. | learnings |

## De-dupe clusters

**Cluster A — "small splits / POC metrics are smoke-test evidence only"** (low-severity restatement, not full dup).
- Files: `2026-05-29_rav-chexpert-ops.md` ("Small Splits Are Smoke Tests"; Next-Run Checklist), `2026-06-04_rav-mvp-app-ops.md` ("Metrics Need Split And Threshold Provenance", "POC And CheXpert Are Different Claims"), `decisions/data-management.md` (DEC-DAT-002 Consequences: "smoke-test evidence only").
- Recommendation: **Keep all three** — each frames the idea for its own context (ops vs app-display vs decision-consequence). No merge needed. Optionally add a one-line cross-link from the app-ops "Metrics Need Split..." note to the chexpert-ops "Small Splits Are Smoke Tests" note so the canonical statement of the principle is discoverable. Do not fold.

No true content duplication found. The two learnings files have a clean split (training/infra ops vs app/demo ops) and only the shared smoke-test framing overlaps, which is intentional reinforcement.

## Refactor recommendations

- **`decisions/data-management.md` — `canonical: false` on the only decisions file.** No other decisions file exists to be canonical instead; an accepted ADR collection is the canonical record of those decisions. Change `canonical: true` (or confirm intent). Why: `canonical: false` implies a more-authoritative copy exists elsewhere, which is misleading.
- **`2026-05-29_rav-chexpert-ops.md` — missing `related` is empty (`related: []`) but content tightly couples to the app-ops learning and the data-management ADR.** Add `related:` entries pointing to `docs/knowledge-base/learnings/2026-06-04_rav-mvp-app-ops.md` and `docs/knowledge-base/decisions/data-management.md`. The app-ops file already back-links to this one; the link is currently one-directional.
- **`2026-06-04_rav-mvp-app-ops.md` — missing `tags` for `grounding`/`openai`/`report-generation`** despite the "LLM Output Must Stay Grounded" section being a substantive concern. Add tags (`grounding`, `report-generation`) for discoverability. Minor.
- **Frontmatter `created` vs filename date drift — `2026-05-29_rav-chexpert-ops.md`** has `created: 2026-05-29` matching filename but `updated: 2026-06-04` pulling in three 2026-06-04 retro sources. Fine as-is, but the file now straddles two sessions; consider noting in body that 06-04 sources were folded in. Cosmetic.
- **No `id`/work-item field in ADR headers beyond the `[DEC-DAT-NNN]` inline tags.** The `high_water_mark: 2` in metadata is good. No change required; flagging that the convention (inline DEC-IDs + high_water_mark) is consistent and should be preserved if a `reference/` layer is added later.

## Archive/stale candidates

- **None.** All three files describe current MVP state (CheXpert-small local primary, Streamlit demo, GCP spot runner) consistent with the 2026-06 retrospectives, all of which exist on disk. The brain-tumor route is explicitly "deferred rather than rejected," so DEC-DAT-001 is live, not stale. Dates are recent (within ~3 weeks of review date). Nothing to archive.

## Promotion candidates

These are **flagged ONLY** — not to be merged in this pass.

- `docs/knowledge-base/learnings/2026-05-29_rav-chexpert-ops.md` → **target `ai-coding-agents`** — generality **medium**.
  - Rationale: Two sections are reusable cross-project ML-ops doctrine independent of CheXpert/RAV:
    - **"Three Freshness Gates Before Expensive GCP Runs"** (image freshness, data freshness, accelerator freshness — guards against stale-image FileNotFoundError, partial-dataset surprise, silent CPU fallback). This is a generic remote-GPU-job pre-flight pattern.
    - **"`RUN_ID` Controls Resume Semantics"** (same RUN_ID to resume checkpoint sync, new RUN_ID for distinct runs). Generic spot/preemptible training resume hygiene.
  - These belong as a distilled, dataset-agnostic learning in the global KB. The CheXpert-specific sections (thresholds, prep-script timing) stay project-local.
- `docs/knowledge-base/learnings/2026-06-04_rav-mvp-app-ops.md` → **target `ai-coding-agents`** — generality **medium-low** (rate **medium**).
  - Rationale: Two patterns generalize beyond RAV:
    - **"Interpreter-Qualified Streamlit Commands"** (`python -m streamlit` not bare `streamlit` to avoid wrong Conda/.venv Torch dylibs) — a concrete env-isolation gotcha relevant to any Conda+venv+Torch machine. Overlaps with the user's global ".env / interpreter discipline" doctrine.
    - **"LLM Output Must Stay Grounded"** (LLM wrapper answers only from the inference payload; state missing context rather than invent) — generic RAG/grounding discipline for any LLM-over-structured-data feature.
  - Flag for extraction; the Streamlit-specific and RAV-specific framing stays local.

No `personal-automation` candidates. (Sean's-workstation access detail is referenced but the file correctly says to keep that in the "global workstation reference," not here — so it is already pointed at the right home and is not itself a promotion candidate from this KB.)

## Notes

- **Cross-repo overlap suspicions:**
  - The "Three Freshness Gates" + "RUN_ID resume" GCP-spot-training patterns likely overlap with any other RAV-adjacent or ML-training repo the user has, and with global agent-coding doctrine on remote-job pre-flight. Worth de-duping at the cross-repo synthesis step rather than within RAV.
  - The "interpreter-qualified command" gotcha (`python -m X` over bare `X` to dodge wrong Conda/.venv dylibs) is the same class as the user's global `.env`/interpreter rules in `ai-coding-agents`; check for an existing global note before promoting to avoid creating a duplicate.
  - "Sean's workstation" local-GPU lane points to a "global workstation reference" that lives outside this KB — confirm that reference exists and is the canonical home before any local duplication accretes here.
- **Layer health:** Only `decisions/` and `learnings/` are populated. No `reference/` (current-state) or `qa/` (troubleshooting) layers. Several learnings sections (freshness gates, prepare-script timing checks, missing-artifact handling) are troubleshooting-shaped and could seed a `qa/` entry later, but that is net-new authoring, not a refactor of existing files.
- **Source integrity:** All five distinct retrospective sources referenced across the three files exist on disk under `docs/retrospectives/` — cross-links are valid, no dangling references.

---

## Feeder sweep — retros / reviewer-notes / state files (2026-06-21)

> Safety-net pass over feeder docs (most retro content was already extracted to KB by closeout skills). Counts: retros 9, reviewer-notes 2, state-files 5.

**Result:**

- Clean — all 9 retro findings already extracted to the two RAV-local KB learnings files (docs/knowledge-base/learnings/2026-05-29_rav-chexpert-ops.md and 2026-06-04_rav-mvp-app-ops.md). Closeout-skill extraction was complete; no retro_gaps and no local cleanup needed.
- Reviewer notes (all_notes.md + 20260617_pr-5-deepseek.md) contain a single clean PR-5 DeepSeek review with 0 findings across all severities and no carry-forward items — zero reusable yield, zero sensitive content.
- State files (5, all in .agent-sessions/closed/) carry only machine fields + closeout pointers, no handoff-brain prose and no recurring cross-session gotchas — expected zero yield.
- Retro 'Global Candidates' sections are ML-domain-specific ops patterns (spot GPU training freshness gates, threshold-before-backbone tuning, small-vs-full dataset onboarding, RUN_ID resume semantics) — correctly project/domain-scoped to RAV's KB, not universal agent-coding doctrine; global KB confirmed to hold no ML-training-ops doctrine, so nothing was mis-filed.
- One borderline pattern (interpreter-qualified `python -m streamlit` to dodge Conda/.venv Torch dylib conflicts) is generalizable to mixed Conda/venv ML machines but is already adequately covered by global reference 2026-06-05_conda-miniforge-workstation-baseline.md; not worth promoting.
